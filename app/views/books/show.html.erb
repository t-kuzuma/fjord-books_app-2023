<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('book_actions.edit'), edit_book_path(@book) %> |
  <%= link_to t('book_actions.back'), books_path %>

  <%= button_to t('book_actions.destroy'), @book, method: :delete %>
</nav>
