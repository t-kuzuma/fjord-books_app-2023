<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.edit'), edit_book_path(@book) %> |
  <%= link_to t('books.back'), books_path %>

  <%= button_to t('books.destroy'), @book, method: :delete %>
</nav>
